# Blog-2

- Pick: If need to create a new interface or use some property from a existing master interface we use the pick for this.

- Omit: It wark oposite way of pick. it create a new interface by removing specific property.

For those our code will less duplicate, easier to refactoring, consistency will good, sefer for management and keep our code DRY (Don's Repeat Yourself).
